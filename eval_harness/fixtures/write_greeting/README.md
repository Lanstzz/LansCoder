# write-greeting fixture

This intentionally tiny, human-reviewable fixture is the seed workspace for
the offline `write_greeting` interaction replay case. The runner copies it
into a fresh artifact directory before the current LansCoder runtime executes.
